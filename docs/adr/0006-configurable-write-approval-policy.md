# Configurable write approval policy

PaiCLI will require approval by default for high-risk tools while allowing ordinary workspace writes to proceed with audit logging. Projects that need stricter control can enable approval for every non-read-only tool, preserving an efficient default workflow without removing the compliance-oriented option.
